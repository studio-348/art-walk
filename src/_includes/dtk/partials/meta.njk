<div>
    <h1>{{ title }}</h1>
</div>
<div>
    {% artist artist %}
    <p class="meta-text"><span class="meta-bold">Year:</span> <span>{{created}}</span>
        {% if decommissioned %}&nbsp; &nbsp; <span class="meta-bold">Decom:</span> <span>{{decommissioned}}{% endif %}
    </p> 
    <p class="meta-text"><span class="meta-bold">Medium:</span> <span>{{medium}}</span></p>
    {% location location, city %}
</div>